<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Reset Your Netflix Lite Password</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family:
          'Inter',
          -apple-system,
          BlinkMacSystemFont,
          'Segoe UI',
          Roboto,
          sans-serif;
        line-height: 1.6;
        background: linear-gradient(
          135deg,
          #141e30 0%,
          #243b55 50%,
          #0f3460 100%
        );
        min-height: 100vh;
        padding: 20px;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(20px);
        border-radius: 24px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        overflow: hidden;
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
      }

      .header {
        background: linear-gradient(
          135deg,
          #ff6b6b 0%,
          #ee5a24 50%,
          #e55039 100%
        );
        padding: 40px 30px;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .header::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
          circle,
          rgba(255, 255, 255, 0.1) 0%,
          transparent 70%
        );
        animation: pulse 4s ease-in-out infinite;
      }

      @keyframes pulse {
        0%,
        100% {
          transform: scale(1);
          opacity: 0.3;
        }
        50% {
          transform: scale(1.1);
          opacity: 0.6;
        }
      }

      .logo {
        font-size: 32px;
        font-weight: 700;
        color: white;
        margin-bottom: 8px;
        position: relative;
        z-index: 2;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      }

      .subtitle {
        color: rgba(255, 255, 255, 0.9);
        font-size: 16px;
        font-weight: 300;
        position: relative;
        z-index: 2;
      }

      .security-icon {
        width: 60px;
        height: 60px;
        background: linear-gradient(
          135deg,
          rgba(255, 255, 255, 0.2) 0%,
          rgba(255, 255, 255, 0.1) 100%
        );
        border-radius: 50%;
        margin: 20px auto 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        position: relative;
        z-index: 2;
        border: 2px solid rgba(255, 255, 255, 0.2);
      }

      .content {
        padding: 40px 30px;
        color: white;
      }

      .greeting {
        font-size: 28px;
        font-weight: 600;
        margin-bottom: 20px;
        background: linear-gradient(135deg, #fff 0%, #e0e0e0 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .message {
        font-size: 16px;
        color: rgba(255, 255, 255, 0.8);
        margin-bottom: 30px;
        line-height: 1.7;
      }

      .alert-box {
        background: linear-gradient(
          135deg,
          rgba(255, 107, 107, 0.15) 0%,
          rgba(238, 90, 36, 0.1) 100%
        );
        border: 1px solid rgba(255, 107, 107, 0.3);
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 30px;
        border-left: 4px solid #ff6b6b;
      }

      .alert-text {
        font-size: 14px;
        color: rgba(255, 255, 255, 0.9);
        display: flex;
        align-items: center;
      }

      .alert-text::before {
        content: '⚠️';
        margin-right: 10px;
        font-size: 16px;
      }

      .code-section {
        background: linear-gradient(
          135deg,
          rgba(255, 107, 107, 0.1) 0%,
          rgba(238, 90, 36, 0.05) 100%
        );
        border: 2px solid rgba(255, 107, 107, 0.3);
        border-radius: 16px;
        padding: 30px;
        text-align: center;
        margin: 30px 0;
        position: relative;
      }

      .code-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
          135deg,
          rgba(255, 107, 107, 0.1) 0%,
          transparent 50%
        );
        border-radius: 14px;
        pointer-events: none;
      }

      .code-label {
        font-size: 14px;
        color: rgba(255, 255, 255, 0.6);
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 15px;
        font-weight: 500;
      }

      .reset-code {
        font-size: 36px;
        font-weight: 700;
        color: #ff6b6b;
        font-family: 'Monaco', 'Consolas', monospace;
        letter-spacing: 8px;
        margin: 10px 0;
        text-shadow: 0 0 20px rgba(255, 107, 107, 0.5);
        position: relative;
        z-index: 2;
      }

      .code-info {
        font-size: 13px;
        color: rgba(255, 255, 255, 0.5);
        margin-top: 15px;
        font-style: italic;
      }

      .security-notice {
        background: rgba(0, 0, 0, 0.2);
        border-radius: 12px;
        padding: 20px;
        margin: 30px 0;
        border-left: 4px solid #4ecdc4;
      }

      .security-notice h4 {
        color: #4ecdc4;
        font-size: 16px;
        margin-bottom: 10px;
        font-weight: 600;
      }

      .security-notice p {
        color: rgba(255, 255, 255, 0.7);
        font-size: 14px;
        line-height: 1.6;
      }

      .cta-section {
        text-align: center;
        margin: 30px 0;
      }

      .cta-button {
        display: inline-block;
        background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
        color: white;
        padding: 16px 40px;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        font-size: 16px;
        transition: all 0.3s ease;
        box-shadow: 0 8px 25px rgba(255, 107, 107, 0.3);
        border: 1px solid rgba(255, 255, 255, 0.1);
      }

      .cta-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 35px rgba(255, 107, 107, 0.4);
      }

      .footer {
        text-align: center;
        padding: 20px 30px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(0, 0, 0, 0.2);
      }

      .footer-text {
        color: rgba(255, 255, 255, 0.4);
        font-size: 14px;
      }

      .support-link {
        color: #4ecdc4;
        text-decoration: none;
        font-weight: 500;
      }

      .support-link:hover {
        text-decoration: underline;
      }

      @media (max-width: 480px) {
        .container {
          margin: 10px;
          border-radius: 16px;
        }

        .header,
        .content,
        .footer {
          padding: 20px;
        }

        .greeting {
          font-size: 24px;
        }

        .reset-code {
          font-size: 28px;
          letter-spacing: 4px;
        }
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        <div class='logo'>NETFLIX LITE</div>
        <div class='subtitle'>Password Reset Request</div>
        <div class='security-icon'>🔒</div>
      </div>

      <div class='content'>
        <div class='greeting'>Hi {{name}},</div>

        <div class='message'>
          We received a request to reset the password for your Netflix Lite
          account. If you made this request, please use the verification code
          below to proceed with your password reset.
        </div>

        <div class='alert-box'>
          <div class='alert-text'>
            If you didn't request this password reset, please ignore this email
            and contact our support team immediately.
          </div>
        </div>

        <div class='code-section'>
          <div class='code-label'>Password Reset Code</div>
          <div class='reset-code'>{{code}}</div>
          <div class='code-info'>This code expires in 15 minutes</div>
        </div>

        <div class='cta-section'>
          <a href='#' class='cta-button'>Reset Password</a>
        </div>

        <div class='security-notice'>
          <h4>🛡️ Security Tips</h4>
          <p>
            • Never share your verification codes with anyone<br />
            • Netflix Lite will never ask for your password via email<br />
            • Use a strong, unique password for your account<br />
            • Enable two-factor authentication for extra security
          </p>
        </div>
      </div>

      <div class='footer'>
        <div class='footer-text'>
          Need help? Contact our
          <a href='#' class='support-link'>Support Team</a><br />
          Netflix Lite Security Team<br />
          © 2025 Netflix Lite. All rights reserved.
        </div>
      </div>
    </div>
  </body>
</html>