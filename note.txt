config DLQ
haven't try catch important tasks

handle logic upload thumbnail
api notification at api gateway: just for dev
write lib dto, interface

upload thumbnail or automatic generation
create watchlist when user register: get a default picture or url.

comment: mention -> notification.
search service: api search user theo tên -> mention (in Zalo)
update content: publish ? yes -> notification
create account -> mailer welcome, confirm account, status account in entity users
update content ?: if publish (null -> !null) -> notification to user interested
user click interested a movie or series -> update their interested list (list of contents). When update content -> find and notice
UI at '/' of server.
create, update content,.... -> notification
prod: 
    - private bucket and GET /video/:videoId/stream return master.m3u8 instead or use nginx

Season: 'Spring' | 'Summer' | 'Autumn' | 'Winter
quantity: '360p', '720p', 'HD', 'HD+', 'fullHD', 'fullHD+', '2K', '4K', '8K'

write API design 
frontend: handle 3 replies level